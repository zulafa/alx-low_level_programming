#include "main.h"

/**
 * get_endianness - checks if a machine
 * is big or not big.
 *
 * Return: 0 for big, 1 for little.
 */
int get_endianess(void)
{
unsigned int i = 1;
char *c = (char *) &i;

return (*c);
}
